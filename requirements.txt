# Dependencias del proyecto completo.
#
#   python -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt     (Windows)
#   .venv/bin/pip install -r requirements.txt         (Linux / macOS)
#
# Requiere Python 3.14.
#
# Las versiones de la seccion de modelos estan ancladas a proposito. Un .joblib
# de scikit-learn solo garantiza cargar con la version con la que se serializo:
# subirla sin reentrenar puede romper la carga o, peor, cargarla y predecir
# distinto sin avisar.

# --- API de tasacion ---------------------------------------------------------
# Es lo unico imprescindible para consultar los modelos ya entrenados.
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
scikit-learn==1.9.0
xgboost==3.4.1
numpy==2.5.2
pandas==3.0.5
scipy==1.18.0
joblib==1.5.3

# --- Extraccion de avisos ----------------------------------------------------
# httpx lo usan tanto el scraper de volumen como el extractor por URL.
# BeautifulSoup y lxml solo los necesita el scraper de volumen: el extractor
# parsea con json y re de la libreria estandar para caber liviano en el contenedor.
httpx==0.28.1
beautifulsoup4>=4.12
lxml>=5.0

# --- Reentrenar desde los notebooks ------------------------------------------
lightgbm==4.7.0
matplotlib==3.11.1
jupyterlab==4.6.3
ipykernel==7.3.0
nbformat==5.11.1

# --- Pruebas -----------------------------------------------------------------
pytest>=8.0

# --- Consultar Google Places -------------------------------------------------
# Solo para servicios_cercanos.ipynb. La cache ya viene en data/poi_cache.sqlite.
python-dotenv==1.2.2
