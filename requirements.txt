# Dependencias del proyecto de modelos ML.
#
#   python -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt     (Windows)
#   .venv/bin/pip install -r requirements.txt         (Linux / macOS)
#
# Requiere Python 3.14.
#
# Las versiones estan ancladas a proposito. Un .joblib de scikit-learn solo
# garantiza cargar con la version con la que se serializo: subir scikit-learn o
# xgboost sin reentrenar puede romper la carga o, peor, cargarla y predecir
# distinto sin avisar.

# --- servir la API -----------------------------------------------------------
# Es lo unico imprescindible para consultar los modelos ya entrenados.
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
scikit-learn==1.9.0
xgboost==3.4.1
numpy==2.5.2
pandas==3.0.5
scipy==1.18.0
joblib==1.5.3

# --- reentrenar desde los notebooks ------------------------------------------
# Solo hace falta si se van a ejecutar modelo_venta.ipynb / modelo_arriendo.ipynb.
lightgbm==4.7.0
matplotlib==3.11.1
jupyterlab==4.6.3
ipykernel==7.3.0
nbformat==5.11.1

# --- volver a consultar Google Places ----------------------------------------
# Solo para servicios_cercanos.ipynb. La cache ya viene en data/poi_cache.sqlite,
# asi que normalmente no hace falta ni la clave ni estas dos librerias.
httpx==0.28.1
python-dotenv==1.2.2
